{erl_opts, [debug_info]}.
{deps, [
  {cowboy, "2.9.0"}
]}.

{shell, [
  {config, [
      {application, my_app, [
          {env, [
              {node_name, os:getenv("NODE_NAME")},
              {cookie, os:getenv("COOKIE")}
          ]}
      ]}
  ]}
]}.
